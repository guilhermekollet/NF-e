#ifndef _DESCONTO_HPP
#define _DESCONTO_HPP
#include <string>
#include <vector>

using namespace std;

class Desconto
{

    //Attributes
    protected:
    vector<Desconto> Lista;

    //Methods
    public:
    Desconto();
    ~Desconto();

};

#endif